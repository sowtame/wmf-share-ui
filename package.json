{
  "name": "dynamic-remotes",
  "private": true,
  "workspaces": [
    "./app1",
    "./app2",
    "./app3"
  ],
  "version": "0.0.0",
  "scripts": {
    "start": "lerna run --scope=app* --parallel start",
    "build": "lerna run --scope=dynamic-remotes_app* --parallel build",
    "serve": "lerna run --scope=dynamic-remotes_app* --parallel serve",
    "clean": "lerna run --scope=dynamic-remotes_app* --parallel clean",
  },
  "devDependencies": {
    "lerna": "3.22.1",
    "wait-on": "7.0.1"
  }
}
